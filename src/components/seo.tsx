import Head from "next/head";

interface SEOProps {
  title: string;
  url: string;
  description: string;
}

const SEO = ({ title, url, description }: SEOProps) => {
  return <div>SEO WORK PAGE</div>;
};

export default SEO;
